version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v   # just to check Node version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
